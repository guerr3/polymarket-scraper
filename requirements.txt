# Core
aiohttp>=3.9.0
asyncpg>=0.29.0
pydantic>=2.5.0
typer>=0.9.0
rich>=13.0.0

# HTML fallback (optional, install separately)
playwright>=1.40.0

# Dev / testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
